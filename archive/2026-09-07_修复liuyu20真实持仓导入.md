# 修复 liuyu20 真实持仓数据并导入数据库

## 背景 / 需求（老板 2026-09-07）

老板要求：①修好 `data/portfolio_liuyu20.yaml.org`；②把真实持仓导入数据库；③现金反推使总资产匹配真实账户。老板拍板：入库目标 = 模拟盘 `sim_positions`，现金反推。

## 根因（已坐实）

`portfolio_liuyu20.yaml.org` 两处问题：

1. **YAML 缩进错误**（第 104 行）：兆易创新 `- code:` 前面多一个空格，整份文件 `yaml.safe_load` 抛 `ParserError at line 104`。
2. **兆易创新(603986)整条误拷**：从「工业有色ETF」复制后只改 code/name/quantity/cost_price，其余全错（`asset_type:""`、`industry:工业有色`、`style:资源周期`、止损 1.40、止盈 2.376、thesis/失效条件全是 ETF 的）。

## 改了什么

### 1. 修复 `data/portfolio_liuyu20.yaml.org`（数据）
- 缩进对齐；兆易创新纠正为 `asset_type:stock`、`industry:半导体`、`style:科技成长`、thesis 改为存储芯片(NOR Flash/DRAM)+MCU 龙头、`stop_loss 330.36`（60 日最低 337.1 下方 2%）、`take_profit 524.92`（成本×1.30）、失效条件三条改半导体逻辑。

### 2. 导入 `sim_positions`（数据，一次性脚本，insert 幂等、禁 delete）
- liuyu20 下 insert 5 条未平持仓（中国移动/小米/腾讯/工业有色ETF/兆易创新），带 `currency`/`asset_type`/`market` 及止损止盈 thesis。
- 现金：最终定为老板确认的**真实现金 149,539**（初版曾用 total_assets 反推 ≈548,437，后老板提供精确值作废）。

### 3. 修复 `scripts/sim_trade.py`（代码，两处）
- **`sync_to_yaml`**：`asset_type`/`currency` 原先硬编码 `"stock"`/`"CNY"`，改为 `p.get(...) or ...`。否则港股被投影成人民币、ETF 被投影成个股。
- **`get_account`**：① `market` 原先写死 `"A股"`，改为 `p.get("market")`——这是「港股查不到实时价」的真正根因（行情源 tencent 是支持港股的，写死 A股 才查不到）；② 加 HKD→CNY 汇率折算，`position_value`/`total_assets`/`unrealized_pnl` 折 CNY，避免 HKD 与 CNY 混加。

### 4. 后端重启
- `api_server` 常驻进程已重启（PID 31597 → 9837），`/api/health` 200、`dev_user=""`（CAS 模式不变）。

## 怎么验证
1. `.org` 文件 `yaml.safe_load` 通过，5 持仓 + 4 watchlist 读出。
2. `get_account('liuyu20')`：港股实时价腾讯 441.8(HKD)/小米 27.6(HKD)、ETF 1.594(CNY)；现金 149,539、initial_cash 104 万。
3. `py_compile sim_trade.py` 通过；`sync_to_yaml('liuyu20')` 投影港股标 HKD、ETF 标 etf。
4. 后端重启后 `/api/health` 200，`/api/trade/account`、`/api/portfolio` 均 200。

## 局限 / 待办（未静默，需老板知悉）
- **本金口径已确认**：老板确认本金 104 万（2026-05-31 追加 30 万至 134 万，后转出 30 万）。真实现金 149,539 已写入 `.org`（cash_by_currency.CNY）与 `sim_accounts.cash`，`initial_cash` 改为 104 万。
- **系统市值 vs 券商市值有 1.36 万口径差**：系统用 tencent 实时价 + fx 0.856 算市值 79.3 万，老板券商显示 80.7 万，差来自港股股价时点 + 汇率取市场价/中间价。不影响「亏损约 8.35 万」结论。
- **反推现金随市值波动**：模拟账户 cash 是静态的，市值每天变，明天股价动、`total_assets` 就会偏离当前值。这是「静态 cash + 动态市值」的固有限制（手动组合 portfolio.yaml 是每次动态反推，不受此限）。
- **watchlist 未导入**：`.org` 里 4 条观察池（恒通股份/兖矿能源/华安黄金ETF/长江电力）本次未动，需要一并导入请说。
- **兆易创新止损/止盈/thesis 是草稿**：330.36 / 524.92 / 逻辑按既有口径推的，老板有具体止损位请告知。
- `scripts/sim_trade.py` 改动未提交 git（守则：未要求不 commit）。

## 追加：腾讯持仓成本修正（2026-09-07 后续）

老板补充：腾讯实际 500 股，分两批买入——400 股 @ 471 HKD + 100 股 @ 557 HKD，加权成本 488.2 HKD。此前 `.org` 误记 400 股 @ 501。

- 已修正 `.org` / `sim_positions` / `portfolio_liuyu20.yaml` 三处：quantity 400→500、cost_price 501→488.2。
- 核算（现价 438.4 HKD，今日收盘 −0.99%）：浮亏 −24,900 HKD（−10.20%），折 CNY ≈ −21,314。其中 400@471 那笔 −6.92%、100@557 那笔 −21.29%。
- 老板确认券商市值 807,001 已含 500 股腾讯，总账结论不变：本金 104 万 → 总资产 956,540，累计亏损约 8.35 万。

# 数据导入体检：9-10 批次无错误 + 可投池缩水复盘

日期：2026-09-11

## 检查范围与方法

老板要求检查「今天的数据导入是否有错误」。实际系统时间 2026-09-11（周五）11:22，今天白天到此刻**只跑了情绪预计算**，完整的数据导入批次是**昨晚 9-10（周四）**跑的（crontab 工作日 11:37 起才开始当天的 import-macro-news）。

方法：读 `logs/import.log` / `precompute.log` / `sentiment_precompute.log`，并用 venv python + pymongo 直接扫 MongoDB 各集合核对 9-10 数据。

## 结论

**9-10 导入批次全部成功、无报错**；数据层 9-10 各集合日期齐全、持仓股日线完整。日线覆盖的缩水是上周日 cron 路径断掉的历史遗留，已在 9-10 修复（见下）。

## 一、今天早上（9-11）跑的任务

`sentiment_precompute` 9:00 / 9:45 / 10:30 / 11:15 共 4 次，全部正常：成功 5、跳过 1（560860 ETF 不做新闻情绪）、去重后 6 标的，无异常。

## 二、昨晚（9-10）导入批次各任务结果

| 任务 | 结果 |
|---|---|
| import-universe-quotes（日线） | 3732/3732 成功，0 失败 |
| import-stock-moneyflow（资金流） | 2248 成功 |
| import-stock-forecast（一致预期） | 2236 成功 |
| import-macro-news（宏观新闻） | 成功（Tushare major_news 无权限 → 降级 akshare_eastmoney） |
| import-top-list | 成功 |
| import-market-moneyflow | 成功 |
| import-industry-moneyflow | 成功 |
| import-hk-shortsell / hk-southbound / dividend-yield / hsgt-flow | 成功 |
| precompute_history（21:00 收盘） | meta/trends/factors 各 3256（候选 3307，615s） |

数据层抽查（MongoDB 各集合 `2026-09-10` 条数）：日线 3735、趋势 3256、因子 3256、meta 3256、资金流 2248、股息 4098、涨停池 53、行业资金流 111、宏观新闻 365、南向 4。**持仓股 600941 / 603986 / 300750 / 00700 / 01810 / 560860 的 9-10 日线全部齐全。**

## 三、发现的问题：日线覆盖缩水（历史遗留，9-10 已修）

`stock_daily_quotes` 日线覆盖从 9-04 的 6103 只一路缩到 9-10 的 3735 只：

```
9-04: 6103 → 9-07: 4508 → 9-08: 3876 → 9-09: 3840 → 9-10: 3735
```

根因（归档 `2026-09-10_成交额每日刷新与港股单位修复.md` 已记录并修复）：

- `latest_amount`（成交额）原本只在周日 `import-universe-light` 更新，而该周日任务因 crontab 历史用了相对路径 `venv/bin/python`（log 里 47 个 `No such file or directory`）一直没跑成，成交额停在 9/4 快照。
- 工作日 `import-universe-quotes` 用陈旧成交额选可投池，越选越偏 → 缩水。
- 9-10 已修：新增 `import_latest_amount` 每天 18:55 刷新成交额（当天刷 5560 只），`import_universe_quotes` 开头也加了兜底。

当前 3735 只属正常水位：可投池定义「成交额 ≥ 5000 万」，缩量日即此量级（放量日回到 ~5700），非错误。

## 四、残留 / 待确认

- `precompute` 候选 3307 vs 完成 3256，差 51 只被跳过（数据不足/财务缺失），属正常容差。
- `stock_signals` 资金流只覆盖 2248 只，另有 2419 只 `moneyflow_date` 为空 —— 系 `import_stock_moneyflow` 只导「成交额前 3000 + 市值 50 亿 + PE>0」的可投池所致，是否符合预期待老板确认（若要全市场资金流需放宽 limit 或去成交额门槛）。

## 本次无代码改动

本次仅做数据体检，未改任何代码/数据，无需回归验证。
